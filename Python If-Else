import math
import os
import random
import re
import sys


if __name__ == '__main__':
    n = int(input().strip())

# check if the n is odd
if n % 2 != 0:
    print("Weird")
    
# check if the n is even     
elif n % 2 == 0:

    # check if n in range from (2 to 5)
    if n in range(2,6):
        print("Not Weird")
        
    # check if n in range from (6 to 20)    
    elif n in range(6,21):
        print("Weird")
        
    # check if n is greater than 20  
    elif n > 20 :
        print("Not Weird")
